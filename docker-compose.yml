version: "3"

services:
  app:
    build:
      context: .
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
    command: >
      sh -c "python manage.py wait_for_db && 
            python manage.py migrate && 
            python manage.py runserver 0.0.0.0:8000"
    environment:
      - DB_HOST=eb-test-pg-jmu.cahuuu9fzerr.us-east-1.rds.amazonaws.com
      - DB_NAME=app
      - DB_USER=postgres
      - DB_PASS=e4dRdSNLm9XbJpah2iuz
